/**
* @file
* Some definitions for default compilation.
*/
#ifndef _constants
#define _constants

// If PARAVER_OUTPUT is defined then the simulation will create a paraver trace.
#ifndef PARAVER_OUTPUT
#define PARAVER_OUTPUT 1	///< If non-zero, creates a paraver output file.
#endif

// #ifndef DEBUG
// #define DEBUG
// #endif

#endif /* _constants */
